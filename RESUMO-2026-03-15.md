# Resumo de retomada - 2026-03-15

Projeto: `/home/regis/sistema-delivery-futuro`

## Estado validado hoje

- `git status` limpo.
- Frontend compila com `npm run build --workspace @delivery-futuro/frontend`.
- Backend compila com `npm run build --workspace @delivery-futuro/backend`.
- `ioredis` está instalado e resolvendo na workspace: versão `5.10.0`.
- `scripts/check-redis.sh` existe, mas a execução no sandbox falhou por permissão no Docker socket (`/var/run/docker.sock`), então o bloqueio foi operacional, não por ausência do pacote.

## O que já está implementado no frontend admin

Em `apps/frontend` já estão no código:

- Auth e permissão:
  - `src/components/auth/auth-guard.tsx`
  - `src/components/auth/permission-guard.tsx`
  - `src/lib/auth.ts`
- UI base:
  - `data-table.tsx`
  - `modal.tsx`
  - `text-input.tsx`
  - `kpi-card.tsx`
  - `list-filters.tsx`
  - `row-actions.tsx`
  - toast global com `toast.tsx`, `toast-provider.tsx` e uso em `src/app/layout.tsx`
- Sidebar/admin com seções e permissão.
- Dashboard real em `src/app/(admin)/dashboard/page.tsx`.
- Pedidos:
  - listagem em `orders/page.tsx`
  - criação com carrinho em `orders/new/page.tsx`
  - detalhe em `orders/[id]/page.tsx`
  - formulários de status, pagamento e cancelamento
- Produtos:
  - listagem, edição, detalhe e formulário
- Clientes:
  - listagem, edição, detalhe e formulário
- Estoque:
  - listagem, edição, detalhe e formulário
- Usuários:
  - listagem, edição e formulário
- Páginas admin adicionais:
  - `financial`
  - `reports`
  - `kds`
  - `delivery`
  - `whatsapp`
  - `loyalty`
  - `production`
  - `purchasing`
  - `roles`
- CEP e área de entrega já avançaram além do resumo antigo:
  - formulário de cliente busca CEP, preenche endereço e valida área de entrega
  - criação de pedido permite novo endereço por CEP, valida atendimento e persiste o endereço no cliente
- Áreas de entrega agora têm página `/delivery-areas` com listagem, filtro, modal e o novo `DeliveryAreaForm`.
- `/delivery-areas/[id]` permite editar uma área utilizando os mesmos campos e respeita a permissão `delivery.assign`.
- `DeliveryAreaForm` foi simplificado para inputs diretos com checkbox e botão único, consumindo os endpoints de criação/atualização e exibindo toasts.
- `/purchasing/suppliers/[id]` exibe o `SupplierForm` para editar um fornecedor existente sob permissão `purchasing.view`.
- `SupplierForm` agora existe com campos nome, documento, telefone, e-mail e observações, reutilizado na edição e pronto para criação em `/purchasing/suppliers/new` futuramente.
- `/purchasing/suppliers/[id]/details` mostra dados do fornecedor, compras e recebimentos com o `StatusBadge`, mantendo a mesma permissão `purchasing.view`.
- `/checkout/delivery` valida CEP com `/addresses/zipcode/:cep`, consulta `/delivery/check-zipcode`, e apresenta dados de entrega antes de prosseguir ao pedido.
- Foi criado `PurchasingReceiptForm` para cadastrar recebimentos com itens, relacionar lotes (batch) e fazer chamadas a `/purchasing/receipts` e `/stock/batches`.
- `financial/dashboard` aceita `start`/`end`, retorna somatórios (vendas, recibos, contas a pagar, caixa aberto) no período e totais pendentes.
- A página administrativa `/financial` agora usa `DashboardPeriodFilter` e `KpiCard` com os indicadores entregues pelo novo dashboard filtrável.
- A sidebar ganhou links diretos para `Áreas de entrega`, `Checkout delivery` e `Recebimentos` dentro da seção Operação.
- `AppModule` agora lista os módulos em ordem alfabética (core, delivery, financeiro, etc.) para facilitar a leitura.
- `TablesController` passou a expor endpoints de `tables`, `commands/open`, `commands/:id/close` e `commands/:id` com as permissões de `orders` em vez das antigas rotinas `tables.*`.
- Criado `AddressMapPreview` nos componentes para renderizar um iframe Google Maps quando latitude+longitude estiverem disponíveis.
- `schema.prisma` ampliou `Order`/`CustomerAddress` com `deliveryAreaId`, `customerAddressId`, `createdById`, timestamp extras, relações e índices, garantindo rastreabilidade completa do CEP/cliente nos pedidos. O DTO `CreateCustomerAddressDto` agora aceita apenas os campos que preenchermos via formulário (sem deliveryAreaId/ibge) para manter o backend responsável por identificar e validar o CEP.
- `OrdersService#create` agora valida CEP/área e clientes via `CustomerAddress`, calcula frete com regra de faixa ou DTO, gera `orderNumber` diário e inclui a criação dos relacionamentos (`customerAddress`, `deliveryArea`, `items`, `payments`, `statusLogs`) na transação.
- `CustomersController` expõe `GET /customers/autocomplete/by-contact` e o serviço retorna até 10 clientes com nome/telefone/WhatsApp/e-mail que contenham a query, incluindo endereços para preencher formulários.
- `ReportsController` ganhou `GET /reports/delivery-dashboard`, que entrega totais, filas de entregas, média de tempo e resumo por entregador com base em intervalos `start`/`end`.
- A rota `/reports/delivery-dashboard` agora foca nos pedidos tipo `DELIVERY`, agrupando totais por área, calculando a média entre `dispatchedAt` e `deliveredAt` e retornando o total de entregas no período.
- Foi criada `GET /reports/operations-dashboard`, que retorna totais de mesas, comandos abertos, pedidos em andamento e entregas pendentes para o painel operacional.
- `GET /reports/operations-dashboard` calcula filas do balcão e da cozinha, pedidos prontos e atrasados para o painel operacional.
- Criado `DeliveryAreaChart`, um gráfico de barras com Recharts para visualizar pedidos por área dentro do dashboard de entregas.
- A dashboard admin consome `/reports/delivery-dashboard`, armazena o payload em `deliveryDashboard` e renderiza `DeliveryAreaChart` abaixo do `SalesPeriodChart`.
- A sidebar adicionou o link “Mesas/Comandas” (`/tables`) sob Operação, usando a permissão `orders.view`.
- Criada a página `/tables` com cards de mesas, modal para abrir comandas e ações rápidas (detalhes, novo pedido); o formulário `OpenCommandForm` reaproveita o modal.
- `OpenCommandForm` foi criado com campo de código e chama `/commands/open`, usado no modal das mesas para abrir comandas.
- `MapPicker` adiciona um mock de mapa na criação de pedidos, convertendo cliques em lat/lng e exibindo coordenadas lado a lado.
- Adicionado `OrderThermalPrintButton` para abrir uma janela de impressão com layout térmico (cozinha e balcão) usando dados do pedido; agora aparecem dois botões no detalhe, um para cada modo.

## O que já está implementado no backend

Em `apps/backend` já estão no código:

- `reviews` implementado no Prisma, módulo e seed.
- Prefixo `/api` mantido.
- Expansão de WhatsApp sem remover o painel atual.
- Novos pacotes/módulos:
  - `bot`
  - `orders-core`
  - `menu`
  - `delivery-fees`
  - `delivery-areas`
- WhatsApp:
  - `whatsapp.service.ts` mantém conversas do painel
  - `whatsapp.service.ts` tem `sendText(...)`
  - `whatsapp.controller.ts` preserva endpoints admin e adiciona `/webhook/whatsapp`
  - `whatsapp.module.ts` importa `BotModule`
- `AppModule` atualizado com os módulos novos e antigos.
- `AppModule` agora também importa `AddressesModule`, expondo o endpoint de CEP globalmente.
- `RedisService` em `src/database/redis.service.ts`.
- Entrega/CEP:
  - `DeliveryArea` existe no schema
  - `CustomerAddress` já tem `deliveryAreaId`, `zipCode`, `ibgeCode` e vínculo com `DeliveryArea`
  - serviço de delivery já faz lookup de CEP via ViaCEP
  - serviço de delivery resolve área atendida por faixa de CEP
- `AddressesController` expõe `GET /addresses/zipcode/:cep` para a busca externa de CEP.
- `AddressesService` usa ViaCEP para preencher os campos de endereço e valida CEP incorreto/fora de área.

## Correções importantes em relação ao resumo anterior

O resumo antigo dizia que alguns pontos ainda estavam pendentes, mas o estado atual do código está mais avançado:

- `orders-core` não está mais mock/in-memory.
  - Hoje ele usa `OrdersService` e `PrismaService`.
  - Já cria cliente, cria/endereça endereço quando necessário, cria pedido real e confirma o pedido.
  - Ainda existe um `Map` local para idempotência, mas o pedido já entra no fluxo principal.
- `ioredis` não está mais pendente de instalação.
  - Já está em `apps/backend/package.json`.
  - Também está resolvendo via `node_modules` na workspace.
- CEP, `DeliveryArea`, lookup de CEP e vínculo do endereço com área de entrega já estão implementados.
  - Isso aparece no schema, no backend e nos formulários de cliente/pedido.

## Pendências prováveis para a próxima sessão

Os próximos pontos mais plausíveis para continuar são:

- Validar Redis em ambiente real com Docker liberado.
- Revisar `RedisService`, porque hoje ele fixa `port: 6379` e usa apenas `REDIS_HOST`.
- Configurar variáveis reais do WhatsApp:
  - `WHATSAPP_VERIFY_TOKEN`
  - `WHATSAPP_PHONE_NUMBER_ID`
  - `WHATSAPP_TOKEN`
- Validar runtime com `DATABASE_URL` real, não só build.
- Testar fluxo ponta a ponta:
  - webhook WhatsApp
  - sessão/bot
  - cotação
  - criação de pedido
  - impacto em KDS/estoque/financeiro
- Verificar se a idempotência do `orders-core` deve sair do `Map` em memória e ir para Redis ou banco.
- Consolidar se o módulo antigo `delivery` e o novo `delivery-areas` devem coexistir ou ser unificados.

## Scripts e comandos úteis

- Redis check deste projeto:
  - `./scripts/check-redis.sh`
- O script `schema-safe-change-check.sh` não está neste repositório.
  - Encontrei em: `../erp-restaurante/apps/backend/scripts/schema-safe-change-check.sh`

## Frase curta para amanhã

Use isto para retomar:

`Continuar no sistema-delivery-futuro. Frontend e backend estão compilando. O projeto já tem admin avançado, WhatsApp/bot, orders-core persistindo pedido real, RedisService com ioredis instalado, e CEP/DeliveryArea integrados ao cliente e ao pedido. Próximo passo: validar Redis/Docker e runtime real, configurar env do WhatsApp e testar o fluxo ponta a ponta.`

## Frase ainda mais curta

`Retomar sistema-delivery-futuro no ponto de WhatsApp, Redis e fluxo real de pedidos. Build frontend/backend OK. CEP e DeliveryArea já estão implementados; falta validar operação real.`
